[gdelt]
host=<host ip>
database=<PostgreSQL database name>
user=<PostgreSQL usernama>
password=<PostgreSQL password>

[gdelt_app]
master=spark://<master node IP>:<master node Port>
spark_input_file="<Path to spark.txt>"
bucket=<S3 bucket>
prefix=<S3 folder with source data>
prefix_upload=<S3 folder with target data>
